political_decisions = {
    language_schooling = {
        picture = embrace_minority
        potential = {
            tag = FRA
            any_owned_province = { is_core = BRT }
            BRT = { exists = no }
            NOT = { has_country_flag = enacted_language_schooling     }
        }

        allow = {
            war = no
            NOT = { school_reforms = no_schools }
            invention = social_darwinism
        }

        effect = {
            set_country_flag = enacted_language_schooling
            remove_accepted_culture = picard
            remove_accepted_culture = occitan
            remove_accepted_culture = breton
            remove_accepted_culture = french_canadian
            any_owned = {
                limit = {
                    OR = {
                        is_core = BRT
                        is_core = OCC
                        culture = picard
                    }
                }
                add_province_modifier = {
                    name = language_cultural_unity
                    duration = -1
                }
            }
            random_owned = {
                limit = {
                    BRT = { exists = no }
                }
                BRT = { all_core = { remove_core = BRT } }
            }
            random_owned = {
                limit = {
                    OCC = { exists = no }
                }
                OCC = { all_core = { remove_core = OCC } }
            }
        }
    }